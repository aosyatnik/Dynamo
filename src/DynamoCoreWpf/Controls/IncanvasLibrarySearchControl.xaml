<UserControl x:Class="Dynamo.UI.Controls.IncanvasLibrarySearchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Dynamo.Controls"
             xmlns:views="clr-namespace:Dynamo.UI.Views"
             xmlns:ui="clr-namespace:Dynamo.UI"
             xmlns:resx="clr-namespace:Dynamo.Properties;assembly=DynamoCore"
             mc:Ignorable="d"
             MaxHeight="350"
             Width="250">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:SharedResourceDictionary Source="{x:Static ui:SharedDictionaryManager.SidebarGridDictionaryUri}" />
                <ui:SharedResourceDictionary Source="{x:Static ui:SharedDictionaryManager.DynamoConvertersDictionaryUri}" />
                <ui:SharedResourceDictionary Source="{x:Static ui:SharedDictionaryManager.DynamoColorsAndBrushesDictionaryUri}" />
            </ResourceDictionary.MergedDictionaries>

            <!--Style for every member in found MemberGroup-->
            <Style x:Key="ListBoxIncanvasMembersStyle"
                   TargetType="{x:Type ListBoxItem}"
                   BasedOn="{StaticResource LibraryMembersStyle}">
                <EventSetter Event="PreviewMouseLeftButtonDown"
                             Handler="OnMouseLeftButtonDown" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Name="SearchText"
              Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border CornerRadius="5"
                    Background="{StaticResource LibraryCommonBackground}"
                    Padding="5"
                    Grid.ColumnSpan="2">
                <Grid>
                    <StackPanel Name="SearchIconAndTextBlockGrid"
                                Grid.ColumnSpan="2"
                                Width="Auto"
                                Orientation="Horizontal">
                        <Image x:Name="SearchIcon"
                               Width="25"
                               Height="26"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"
                               Style="{StaticResource SeachIconStyle}"/>

                        <TextBlock Name="SearchTextBlock"
                                   FontSize="14"
                                   IsHitTestVisible="False"
                                   Foreground="{StaticResource DeafultSearchTextBlockText}"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding Path=SearchText, 
                                                    Converter={StaticResource NonEmptyStringToCollapsedConverter}}"
                                   HorizontalAlignment="Center"
                                   Height="18"
                                   Margin="3,0,0,0"
                                   Text="{x:Static resx:Resources.SearchTextBlockText}" />

                    </StackPanel>

                    <TextBox Name="SearchTextBox"
                             Foreground="{StaticResource CommonSidebarTextColor}"
                             Background="Transparent"
                             BorderThickness="0"
                             FontSize="14"
                             IsEnabled="True"
                             VerticalAlignment="Center"
                             Text="{Binding Path=SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             MinWidth="200"
                             CaretBrush="{StaticResource CommonSidebarTextColor}"
                             Margin="26,0,0,-1"
                             TextChanged="OnSearchTextBoxTextChanged" />
                </Grid>
            </Border>
        </Grid>

        <Border Opacity=".9"
                Background="{StaticResource LibraryCommonBackground}"
                Margin="1,2,1,0"
                Grid.Row="1"
                Name="FoundMembers">
            <Border.Visibility>
                <Binding Path="CurrentMode"
                         Converter="{StaticResource ViewModeToVisibilityConverter}"
                         ConverterParameter="LibrarySearchView" />
            </Border.Visibility>
            <ListView Name="CategoryListView"
                      Grid.Row="1"
                      ScrollViewer.CanContentScroll="False"
                      Background="#343434"
                      BorderThickness="0"
                      ItemsSource="{Binding SearchRootCategories}"
                      ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                      SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <!--Found member groups-->
                        <ListBox Background="Transparent"
                                 BorderBrush="Transparent"
                                 BorderThickness="0"
                                 ItemContainerStyle="{StaticResource ListBoxMemberGroupsStyle}"
                                 ItemsSource="{Binding MemberGroups}"
                                 ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                                 Name="MemberGroupsListBox">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Foreground="#989898"
                                                   FontSize="12"
                                                   Margin="16,8,0,8">
                                                            <Run Text="{Binding Prefix, Mode=OneWay}" />
                                                            <Run Text="{Binding GroupName, Mode=OneWay}" />
                                        </TextBlock>
                                        <ListBox Background="Transparent"
                                                 BorderBrush="Transparent"
                                                 ItemContainerStyle="{StaticResource ListBoxIncanvasMembersStyle}"
                                                 ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                                                 ItemsSource="{Binding Members}"
                                                 Name="MembersListBox">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <ContentControl Template="{StaticResource SearchMemberControlTemplate}" />
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}">
                        <Setter Property="Background"
                                Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListViewItem}">
                                    <Border BorderThickness="0,0,0,0"
                                            BorderBrush="#2F2F2F">
                                        <ContentPresenter />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Border>
    </Grid>
</UserControl>
